<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Details - HB Mart</title>
    <link href="https://cdn.bootcdn.net/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .order-details-container {
            max-width: 900px;
            margin: 0 auto;
            padding: 40px 20px;
        }
        .order-info-card {
            background-color: var(--white);
            border-radius: 8px;
            padding: 30px;
            margin-bottom: 25px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
        }
        .order-item {
            display: flex;
            align-items: center;
            gap: 20px;
            padding: 20px;
            border-bottom: 1px solid var(--light-gray);
        }
        .order-item:last-child {
            border-bottom: none;
        }
        .item-image {
            width: 80px;
            height: 80px;
            object-fit: cover;
            border-radius: 8px;
        }
        .item-details {
            flex: 1;
        }
    </style>
</head>
<body>
    <!-- Header Top -->
    <header class="header-top">
        <div class="header-top-content">
            <div class="header-top-text">
                Free delivery on orders over $60 | Same-day delivery available
            </div>
            <div class="header-top-links">
                <span>Welcome, <%= user.username %>!</span>
            </div>
        </div>
    </header>
    
    <!-- Main Header -->
    <header class="header-main">
        <div class="header-main-content">
            <a href="/" class="logo">HB Mart</a>
            
            <div class="search-bar">
                <i class="fas fa-search"></i>
                <input type="text" placeholder="Search for products...">
            </div>
            
            <div class="nav-links">
                <a href="/shopping">
                    <i class="fas fa-store"></i>
                    <span>Shop</span>
                </a>
                <a href="/orders">
                    <i class="fas fa-receipt"></i>
                    <span>My Orders</span>
                </a>
                <a href="/cart">
                    <i class="fas fa-shopping-cart"></i>
                    <span>Cart</span>
                </a>
                <a href="/logout">
                    <i class="fas fa-sign-out-alt"></i>
                    <span>Logout</span>
                </a>
            </div>
        </div>
    </header>
    
    <!-- Main Content -->
    <div class="order-details-container">
        <div style="margin-bottom: 30px;">
            <a href="/orders" style="color: var(--primary); text-decoration: none;">
                <i class="fas fa-arrow-left"></i> Back to Orders
            </a>
        </div>
        
        <h1 style="margin-bottom: 30px;">
            <i class="fas fa-file-invoice"></i> Order #<%= order.id %>
        </h1>
        
        <!-- Order Information -->
        <div class="order-info-card">
            <h3 style="margin-bottom: 20px;">Order Information</h3>
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px;">
                <div>
                    <div style="font-size: 12px; color: var(--dark-gray); margin-bottom: 5px;">Order Date</div>
                    <div style="font-size: 16px; font-weight: 500;">
                        <%= new Date(order.created_at).toLocaleDateString('en-SG', { 
                            year: 'numeric', 
                            month: 'long', 
                            day: 'numeric',
                            hour: '2-digit',
                            minute: '2-digit'
                        }) %>
                    </div>
                </div>
                <div>
                    <div style="font-size: 12px; color: var(--dark-gray); margin-bottom: 5px;">Status</div>
                    <div style="font-size: 16px; font-weight: 500;">
                        <span style="padding: 6px 15px; border-radius: 20px; background-color: #fff3cd; color: #856404;">
                            <%= order.status || 'Pending' %>
                        </span>
                    </div>
                </div>
                <div>
                    <div style="font-size: 12px; color: var(--dark-gray); margin-bottom: 5px;">Total Amount</div>
                    <div style="font-size: 20px; font-weight: bold; color: var(--primary);">
                        $<%= parseFloat(order.total).toFixed(2) %>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Delivery Information -->
        <div class="order-info-card">
            <h3 style="margin-bottom: 20px;">Delivery Information</h3>
            <div>
                <div style="margin-bottom: 10px;">
                    <i class="fas fa-user"></i> <strong><%= order.username %></strong>
                </div>
                <div style="margin-bottom: 10px;">
                    <i class="fas fa-envelope"></i> <%= order.email %>
                </div>
                <div style="margin-bottom: 10px;">
                    <i class="fas fa-map-marker-alt"></i> <%= order.address %>
                </div>
                <div>
                    <i class="fas fa-phone"></i> <%= order.contact %>
                </div>
            </div>
        </div>
        
        <!-- Order Items -->
        <div class="order-info-card">
            <h3 style="margin-bottom: 20px;">Order Items</h3>
            <% if (order.items && order.items.length > 0) { %>
                <% order.items.forEach(item => { %>
                    <div class="order-item">
                        <img src="/images/<%= item.image %>" alt="<%= item.productName %>" 
                             class="item-image"
                             onerror="this.src='https://via.placeholder.com/80x80?text=No+Image'">
                        
                        <div class="item-details">
                            <div style="font-size: 16px; font-weight: 600; margin-bottom: 5px;">
                                <%= item.productName %>
                            </div>
                            <div style="font-size: 14px; color: var(--dark-gray);">
                                $<%= parseFloat(item.price).toFixed(2) %> × <%= item.quantity %>
                            </div>
                        </div>
                        
                        <div style="font-size: 18px; font-weight: bold;">
                            $<%= (parseFloat(item.price) * item.quantity).toFixed(2) %>
                        </div>
                    </div>
                <% }); %>
                
                <!-- Order Summary -->
                <div style="margin-top: 20px; padding-top: 20px; border-top: 2px solid var(--light-gray);">
                    <div style="display: flex; justify-content: space-between; font-size: 20px; font-weight: bold;">
                        <span>Total:</span>
                        <span style="color: var(--primary);">$<%= parseFloat(order.total).toFixed(2) %></span>
                    </div>
                </div>
            <% } %>
        </div>
    </div>
    
    <!-- Footer -->
    <footer class="footer">
        <div class="footer-content">
            <div class="footer-column">
                <h3>About HB Mart</h3>
                <p>Singapore's leading grocery retailer.</p>
            </div>
            
            <div class="footer-column">
                <h3>Shop</h3>
                <ul class="footer-links">
                    <li><a href="/shopping">All Products</a></li>
                </ul>
            </div>
            
            <div class="footer-column">
                <h3>Help & Support</h3>
                <ul class="footer-links">
                    <li><a href="#">FAQs</a></li>
                    <li><a href="#">Contact Us</a></li>
                </ul>
            </div>
            
            <div class="footer-column">
                <h3>My Account</h3>
                <ul class="footer-links">
                    <li><a href="/orders">My Orders</a></li>
                    <li><a href="/logout">Logout</a></li>
                </ul>
            </div>
        </div>
        
        <div class="footer-bottom">
            <p>&copy; 2024 HB Mart. All rights reserved.</p>
        </div>
    </footer>
    
    <script src="/js/main.js"></script>
</body>
</html>
